export const metadata = {
  title: 'Wordle',
  description: 'Guess the 5-letter word in the fewest tries possible.',
};

export default ({ children }: { children: React.ReactNode }) => {
  return children;
};
